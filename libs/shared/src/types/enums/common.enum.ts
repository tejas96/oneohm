/**
 * Common/Generic Enums
 *
 * This file contains generic enums that are reusable across multiple modules.
 * Module-specific enums should be in their respective enum files.
 */

// ============================================================================
// Sorting Enums
// ============================================================================

/**
 * Generic sort order for list APIs
 * Used with sortBy fields to specify ascending or descending order
 */
export enum SortOrder {
  ASC = 'ASC',
  DESC = 'DESC',
}
