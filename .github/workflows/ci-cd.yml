name: CI/CD with NX

on:
  push:
    branches: [main, dev]
  pull_request:
    branches: [main, dev]

# Cancel in-progress runs when a new commit is pushed
concurrency:
  group: ${{ github.workflow }}-${{ github.ref }}
  cancel-in-progress: true

jobs:
  setup:
    name: Setup NX
    runs-on: ubuntu-latest
    outputs:
      has-affected: ${{ steps.affected.outputs.has-affected }}
    steps:
      - name: Checkout code
        uses: actions/checkout@v5
        with:
          fetch-depth: 0

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: '20'
          cache: 'npm'

      - name: Install dependencies
        run: npm ci

      - name: Check affected projects
        id: affected
        run: |
          echo "has-affected=true" >> $GITHUB_OUTPUT

      - name: Generate Setup Summary
        run: |
          echo "## 🚀 NX Setup Complete" >> $GITHUB_STEP_SUMMARY
          echo "" >> $GITHUB_STEP_SUMMARY
          echo "✅ Node.js version: $(node --version)" >> $GITHUB_STEP_SUMMARY
          echo "✅ NPM version: $(npm --version)" >> $GITHUB_STEP_SUMMARY
          echo "✅ Dependencies installed successfully" >> $GITHUB_STEP_SUMMARY
          echo "" >> $GITHUB_STEP_SUMMARY
          echo "**Branch:** ${{ github.ref_name }}" >> $GITHUB_STEP_SUMMARY
          echo "**Commit:** ${{ github.sha }}" >> $GITHUB_STEP_SUMMARY

  lint:
    name: Lint Affected
    needs: setup
    runs-on: ubuntu-latest
    permissions:
      contents: read
      actions: read
    steps:
      - name: Checkout code
        uses: actions/checkout@v5
        with:
          fetch-depth: 0

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: '20'
          cache: 'npm'

      - name: Install dependencies
        run: npm ci

      - name: Derive appropriate SHAs for base and head for `nx affected` commands
        uses: nrwl/nx-set-shas@v4

      - name: Run lint on affected projects
        id: lint-run
        run: |
          npx nx affected --target=lint --parallel=3 || {
            EXIT_CODE=$?
            if [ $EXIT_CODE -eq 0 ]; then
              echo "No affected projects to lint"
              exit 0
            else
              exit $EXIT_CODE
            fi
          }
          npx nx print-affected --select=projects > /tmp/affected-projects.txt || echo "none" > /tmp/affected-projects.txt

      - name: Generate Lint Summary
        if: always()
        run: |
          echo "## 🔍 Linting Results" >> $GITHUB_STEP_SUMMARY
          echo "" >> $GITHUB_STEP_SUMMARY
          if [ -f /tmp/affected-projects.txt ]; then
            echo "**Affected Projects:**" >> $GITHUB_STEP_SUMMARY
            cat /tmp/affected-projects.txt | tr ',' '\n' | sed 's/^/- /' >> $GITHUB_STEP_SUMMARY
          fi
          echo "" >> $GITHUB_STEP_SUMMARY
          if [ "${{ steps.lint-run.outcome }}" == "success" ]; then
            echo "✅ All linting checks passed successfully!" >> $GITHUB_STEP_SUMMARY
          else
            echo "❌ Linting failed. Please check the logs above." >> $GITHUB_STEP_SUMMARY
          fi

  test:
    name: Test Affected
    needs: setup
    runs-on: ubuntu-latest
    permissions:
      contents: read
      actions: read
    steps:
      - name: Checkout code
        uses: actions/checkout@v5
        with:
          fetch-depth: 0

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: '20'
          cache: 'npm'

      - name: Install dependencies
        run: npm ci

      - name: Derive appropriate SHAs for base and head for `nx affected` commands
        uses: nrwl/nx-set-shas@v4

      - name: Run tests on affected projects
        id: test-run
        run: |
          npx nx affected --target=test --parallel=3 --coverage || {
            EXIT_CODE=$?
            if [ $EXIT_CODE -eq 0 ]; then
              echo "No affected projects to test"
              exit 0
            else
              exit $EXIT_CODE
            fi
          }
          npx nx print-affected --select=projects > /tmp/affected-projects-test.txt || echo "none" > /tmp/affected-projects-test.txt

      - name: Upload coverage
        uses: codecov/codecov-action@v4
        if: always()
        with:
          directory: ./coverage
          fail_ci_if_error: false

      - name: Generate Test Summary
        if: always()
        run: |
          echo "## 🧪 Test Results" >> $GITHUB_STEP_SUMMARY
          echo "" >> $GITHUB_STEP_SUMMARY
          if [ -f /tmp/affected-projects-test.txt ]; then
            echo "**Tested Projects:**" >> $GITHUB_STEP_SUMMARY
            cat /tmp/affected-projects-test.txt | tr ',' '\n' | sed 's/^/- /' >> $GITHUB_STEP_SUMMARY
          fi
          echo "" >> $GITHUB_STEP_SUMMARY
          if [ "${{ steps.test-run.outcome }}" == "success" ]; then
            echo "✅ All tests passed successfully!" >> $GITHUB_STEP_SUMMARY
            echo "📊 Coverage reports uploaded to Codecov" >> $GITHUB_STEP_SUMMARY
          else
            echo "❌ Some tests failed. Please check the logs above." >> $GITHUB_STEP_SUMMARY
          fi

  build:
    name: Build Affected
    needs: [lint, test]
    runs-on: ubuntu-latest
    permissions:
      contents: read
      actions: read
    steps:
      - name: Checkout code
        uses: actions/checkout@v5
        with:
          fetch-depth: 0

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: '20'
          cache: 'npm'

      - name: Install dependencies
        run: npm ci

      - name: Derive appropriate SHAs for base and head for `nx affected` commands
        uses: nrwl/nx-set-shas@v4

      - name: Build affected projects
        id: build-run
        run: |
          npx nx affected --target=build --parallel=3 || {
            EXIT_CODE=$?
            if [ $EXIT_CODE -eq 0 ]; then
              echo "No affected projects to build"
              exit 0
            else
              exit $EXIT_CODE
            fi
          }
          npx nx print-affected --select=projects > /tmp/affected-projects-build.txt || echo "none" > /tmp/affected-projects-build.txt

      - name: Check if mobile is affected
        id: check-mobile
        run: |
          if npx nx print-affected --select=projects | grep -q "mobile"; then
            echo "affected=true" >> $GITHUB_OUTPUT
          else
            echo "affected=false" >> $GITHUB_OUTPUT
          fi

      - name: Bundle Mobile iOS
        if: steps.check-mobile.outputs.affected == 'true'
        run: npx nx bundle-ios mobile

      - name: Bundle Mobile Android
        if: steps.check-mobile.outputs.affected == 'true'
        run: npx nx bundle-android mobile

      - name: Upload build artifacts
        uses: actions/upload-artifact@v4
        if: success()
        with:
          name: dist
          path: |
            apps/*/dist
            apps/*/.next
            apps/mobile/android/app/build
            apps/mobile/ios/build
          retention-days: 7

      - name: Generate Build Summary
        if: always()
        run: |
          echo "## 🔨 Build Results" >> $GITHUB_STEP_SUMMARY
          echo "" >> $GITHUB_STEP_SUMMARY
          if [ -f /tmp/affected-projects-build.txt ]; then
            echo "**Built Projects:**" >> $GITHUB_STEP_SUMMARY
            cat /tmp/affected-projects-build.txt | tr ',' '\n' | sed 's/^/- /' >> $GITHUB_STEP_SUMMARY
          fi
          echo "" >> $GITHUB_STEP_SUMMARY
          if [ "${{ steps.check-mobile.outputs.affected }}" == "true" ]; then
            echo "📱 Mobile bundles created for iOS and Android" >> $GITHUB_STEP_SUMMARY
          fi
          echo "" >> $GITHUB_STEP_SUMMARY
          if [ "${{ steps.build-run.outcome }}" == "success" ]; then
            echo "✅ All projects built successfully!" >> $GITHUB_STEP_SUMMARY
            echo "📦 Build artifacts uploaded (7 days retention)" >> $GITHUB_STEP_SUMMARY
          else
            echo "❌ Build failed. Please check the logs above." >> $GITHUB_STEP_SUMMARY
          fi

  deploy-backend:
    name: Deploy Backend
    needs: build
    if: github.event_name == 'push' && github.ref == 'refs/heads/main'
    runs-on: ubuntu-latest
    permissions:
      contents: read
      actions: read
    steps:
      - name: Checkout code
        uses: actions/checkout@v5
        with:
          fetch-depth: 0

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: '20'
          cache: 'npm'

      - name: Install dependencies
        run: npm ci

      - name: Derive appropriate SHAs
        uses: nrwl/nx-set-shas@v4

      - name: Check if backend is affected
        id: check-backend
        run: |
          if npx nx print-affected --select=projects | grep -q "backend"; then
            echo "affected=true" >> $GITHUB_OUTPUT
          else
            echo "affected=false" >> $GITHUB_OUTPUT
          fi

      - name: Build Backend Docker Image
        if: steps.check-backend.outputs.affected == 'true'
        working-directory: ./apps/backend
        run: docker build -t oneohm-epc-backend:latest .

      - name: Deploy Backend
        if: steps.check-backend.outputs.affected == 'true'
        run: echo "Add your backend deployment steps here"

      - name: Generate Backend Deployment Summary
        if: always()
        run: |
          echo "## 🚀 Backend Deployment" >> $GITHUB_STEP_SUMMARY
          echo "" >> $GITHUB_STEP_SUMMARY
          if [ "${{ steps.check-backend.outputs.affected }}" == "true" ]; then
            echo "✅ Backend was affected by changes" >> $GITHUB_STEP_SUMMARY
            echo "🐳 Docker image built successfully" >> $GITHUB_STEP_SUMMARY
            echo "📦 Deployment initiated" >> $GITHUB_STEP_SUMMARY
            echo "" >> $GITHUB_STEP_SUMMARY
            echo "**Environment:** Production" >> $GITHUB_STEP_SUMMARY
            echo "**Branch:** ${{ github.ref_name }}" >> $GITHUB_STEP_SUMMARY
          else
            echo "⏭️ Backend was not affected, skipping deployment" >> $GITHUB_STEP_SUMMARY
          fi

  deploy-web:
    name: Deploy Web
    needs: build
    if: github.event_name == 'push' && github.ref == 'refs/heads/main'
    runs-on: ubuntu-latest
    permissions:
      contents: read
      actions: read
    steps:
      - name: Checkout code
        uses: actions/checkout@v5
        with:
          fetch-depth: 0

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: '20'
          cache: 'npm'

      - name: Install dependencies
        run: npm ci

      - name: Derive appropriate SHAs
        uses: nrwl/nx-set-shas@v4

      - name: Check if web is affected
        id: check-web
        run: |
          if npx nx print-affected --select=projects | grep -q "web"; then
            echo "affected=true" >> $GITHUB_OUTPUT
          else
            echo "affected=false" >> $GITHUB_OUTPUT
          fi

      - name: Build Web Application
        if: steps.check-web.outputs.affected == 'true'
        run: npx nx build web

      - name: Deploy Web
        if: steps.check-web.outputs.affected == 'true'
        run: echo "Add your web deployment steps here (Vercel, Netlify, etc.)"

      - name: Generate Web Deployment Summary
        if: always()
        run: |
          echo "## 🌐 Web Application Deployment" >> $GITHUB_STEP_SUMMARY
          echo "" >> $GITHUB_STEP_SUMMARY
          if [ "${{ steps.check-web.outputs.affected }}" == "true" ]; then
            echo "✅ Web app was affected by changes" >> $GITHUB_STEP_SUMMARY
            echo "🏗️ Build completed successfully" >> $GITHUB_STEP_SUMMARY
            echo "📦 Deployment initiated" >> $GITHUB_STEP_SUMMARY
            echo "" >> $GITHUB_STEP_SUMMARY
            echo "**Environment:** Production" >> $GITHUB_STEP_SUMMARY
            echo "**Branch:** ${{ github.ref_name }}" >> $GITHUB_STEP_SUMMARY
          else
            echo "⏭️ Web app was not affected, skipping deployment" >> $GITHUB_STEP_SUMMARY
          fi

  # Mobile deployment jobs (commented for future use)
  # Uncomment and configure when ready to deploy mobile apps

  # deploy-mobile-ios:
  #   name: Deploy iOS App
  #   needs: build
  #   if: github.event_name == 'push' && github.ref == 'refs/heads/main'
  #   runs-on: macos-latest
  #   permissions:
  #     contents: read
  #     actions: read
  #   steps:
  #     - name: Checkout code
  #       uses: actions/checkout@v5
  #       with:
  #         fetch-depth: 0
  #
  #     - name: Setup Node.js
  #       uses: actions/setup-node@v4
  #       with:
  #         node-version: '20'
  #         cache: 'npm'
  #
  #     - name: Install dependencies
  #       run: npm ci
  #
  #     - name: Derive appropriate SHAs
  #       uses: nrwl/nx-set-shas@v4
  #
  #     - name: Check if mobile is affected
  #       id: check-mobile
  #       run: |
  #         if npx nx print-affected --select=projects | grep -q "mobile"; then
  #           echo "affected=true" >> $GITHUB_OUTPUT
  #         else
  #           echo "affected=false" >> $GITHUB_OUTPUT
  #         fi
  #
  #     - name: Setup Ruby
  #       if: steps.check-mobile.outputs.affected == 'true'
  #       uses: ruby/setup-ruby@v1
  #       with:
  #         ruby-version: '3.0'
  #
  #     - name: Install CocoaPods dependencies
  #       if: steps.check-mobile.outputs.affected == 'true'
  #       run: npx nx pod-install mobile
  #
  #     - name: Build iOS App
  #       if: steps.check-mobile.outputs.affected == 'true'
  #       run: npx nx build-ios mobile
  #       # TODO: Add signing certificates and provisioning profiles
  #       # - Import signing certificate from secrets
  #       # - Import provisioning profile from secrets
  #       # - Configure Xcode build settings
  #
  #     - name: Upload to App Store Connect
  #       if: steps.check-mobile.outputs.affected == 'true'
  #       run: echo "Add App Store Connect upload steps here"
  #       # TODO: Configure App Store Connect credentials
  #       # - Add App Store Connect API key to secrets
  #       # - Use fastlane or altool to upload IPA
  #
  #     - name: Generate iOS Deployment Summary
  #       if: always()
  #       run: |
  #         echo "## 📱 iOS App Deployment" >> $GITHUB_STEP_SUMMARY
  #         echo "" >> $GITHUB_STEP_SUMMARY
  #         if [ "${{ steps.check-mobile.outputs.affected }}" == "true" ]; then
  #           echo "✅ iOS app was affected by changes" >> $GITHUB_STEP_SUMMARY
  #           echo "🍎 iOS build completed successfully" >> $GITHUB_STEP_SUMMARY
  #           echo "📦 Uploaded to App Store Connect" >> $GITHUB_STEP_SUMMARY
  #           echo "" >> $GITHUB_STEP_SUMMARY
  #           echo "**Environment:** Production" >> $GITHUB_STEP_SUMMARY
  #           echo "**Branch:** ${{ github.ref_name }}" >> $GITHUB_STEP_SUMMARY
  #         else
  #           echo "⏭️ iOS app was not affected, skipping deployment" >> $GITHUB_STEP_SUMMARY
  #         fi

  # deploy-mobile-android:
  #   name: Deploy Android App
  #   needs: build
  #   if: github.event_name == 'push' && github.ref == 'refs/heads/main'
  #   runs-on: ubuntu-latest
  #   permissions:
  #     contents: read
  #     actions: read
  #   steps:
  #     - name: Checkout code
  #       uses: actions/checkout@v5
  #       with:
  #         fetch-depth: 0
  #
  #     - name: Setup Node.js
  #       uses: actions/setup-node@v4
  #       with:
  #         node-version: '20'
  #         cache: 'npm'
  #
  #     - name: Setup Java
  #       uses: actions/setup-java@v4
  #       with:
  #         distribution: 'temurin'
  #         java-version: '17'
  #
  #     - name: Install dependencies
  #       run: npm ci
  #
  #     - name: Derive appropriate SHAs
  #       uses: nrwl/nx-set-shas@v4
  #
  #     - name: Check if mobile is affected
  #       id: check-mobile
  #       run: |
  #         if npx nx print-affected --select=projects | grep -q "mobile"; then
  #           echo "affected=true" >> $GITHUB_OUTPUT
  #         else
  #           echo "affected=false" >> $GITHUB_OUTPUT
  #         fi
  #
  #     - name: Build Android App
  #       if: steps.check-mobile.outputs.affected == 'true'
  #       run: npx nx build-android mobile --configuration=release
  #       # TODO: Add signing configuration
  #       # - Add keystore file to secrets
  #       # - Configure signing in gradle.properties
  #       # - Set up keystore password and alias
  #
  #     - name: Upload to Google Play Console
  #       if: steps.check-mobile.outputs.affected == 'true'
  #       run: echo "Add Google Play Console upload steps here"
  #       # TODO: Configure Google Play Console credentials
  #       # - Add service account JSON to secrets
  #       # - Use fastlane or Google Play Publisher API
  #
  #     - name: Generate Android Deployment Summary
  #       if: always()
  #       run: |
  #         echo "## 📱 Android App Deployment" >> $GITHUB_STEP_SUMMARY
  #         echo "" >> $GITHUB_STEP_SUMMARY
  #         if [ "${{ steps.check-mobile.outputs.affected }}" == "true" ]; then
  #           echo "✅ Android app was affected by changes" >> $GITHUB_STEP_SUMMARY
  #           echo "🤖 Android build completed successfully" >> $GITHUB_STEP_SUMMARY
  #           echo "📦 Uploaded to Google Play Console" >> $GITHUB_STEP_SUMMARY
  #           echo "" >> $GITHUB_STEP_SUMMARY
  #           echo "**Environment:** Production" >> $GITHUB_STEP_SUMMARY
  #           echo "**Branch:** ${{ github.ref_name }}" >> $GITHUB_STEP_SUMMARY
  #         else
  #           echo "⏭️ Android app was not affected, skipping deployment" >> $GITHUB_STEP_SUMMARY
  #         fi
