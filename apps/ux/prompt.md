You are an expert full-stack architect and UI engineer with mastery in Tailwind CSS, NestJS, and modern UI/UX systems.

You are provided with the OneOhm ERP/CRM project BRD (file: "brd"). Based on this document, build a **fully interactive front-end design** that reflects the entire system flow and module structure described in the BRD.

🎯 Objective:
Build the **complete interactive UI design** (end-to-end flow) for the OneOhm ERP/CRM platform using **NestJS for structure and Tailwind CSS for styling**.

🧱 Requirements:

- Use **NestJS** only for setting up project structure and routing (no backend logic or APIs).
- Use **Tailwind CSS** for all UI components and styling.
- Include **mock data** in JSON format to populate UI components for testing and demonstration.
- The design must be **interactive**, like a real app (buttons, navigation, modals, sidebars, forms, dashboards, tables, etc.), but **no business logic or API calls**.
- Use **modern UI components and libraries** (e.g., shadcn/ui, Lucide icons, HeadlessUI, DaisyUI) for a clean and professional interface.
- Include a **unified layout system** (sidebar, top bar, breadcrumb navigation, tabs, etc.).
- Add **responsive design** — the web app must adapt perfectly to different screen sizes.
- Include **consistent theming** (dark/light mode optional).
- Use **placeholder/mock content** for data-driven components such as tables, cards, and charts.

📱 Mobile Design:

- Create a **separate mobile design** version using **pure HTML + Tailwind**.
- It should mirror the web UI flow (dashboard, menus, forms, etc.).
- The mobile version must be viewable and interactive directly in the browser (no backend required).

🧩 Output Expectations:

1. **Complete folder structure** for NestJS + Tailwind UI project.
2. **Interactive mock UI pages** for all modules mentioned in the BRD (dashboard, project management, milestones, tasks, users, settings, etc.).
3. **Reusable components** (buttons, cards, modals, data tables, tabs, form controls, icons, etc.).
4. **Navigation flow** between modules with mock data.
5. **Separate HTML+Tailwind folder** for mobile UI (viewable directly in browser).
6. Use **Lucide or Heroicons** for all icons.
7. Ensure **modern, clean, and elegant design system** aligned with ERP/CRM usability standards.

⚠️ Constraints:

- No API integrations.
- No JavaScript logic or business functionality.
- Focus only on **interactive UI design and flow simulation**.
- Use **mock data** and **front-end-only state**.
- Do not include any Figma references.

💡 Goal:
Deliver a complete, interactive, production-ready **frontend-only project design** for OneOhm ERP/CRM that can be directly used as a visual prototype — realistic, modular, and scalable for future backend integration.
