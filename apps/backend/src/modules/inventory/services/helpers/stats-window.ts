/**
 * @deprecated Import from `modules/analytics/common/helpers/stats-window` instead.
 * Kept as a re-export so existing inventory imports continue to work.
 */
export * from '../../../analytics/common/helpers/stats-window';
