// The permission catalog moved to `@/lib/rbac`. It is no longer a bare list of
// strings: codes are a typed union, and each carries the label and description
// the access dialog shows. Import from there.
export {};
