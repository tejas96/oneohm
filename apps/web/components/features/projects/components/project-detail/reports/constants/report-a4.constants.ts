/** A4 page width at 96dpi (210mm). Report templates use this fixed width. */
export const REPORT_A4_WIDTH_PX = 794;

/** Minimum A4 page height at 96dpi (297mm). */
export const REPORT_A4_HEIGHT_PX = 1123;

/** Uniform page margin in mm — applied via body padding in report-print-base.css. */
export const REPORT_PAGE_MARGIN_MM = 12;
