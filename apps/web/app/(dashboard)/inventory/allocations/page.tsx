import { InventoryAllocationsPage } from '@/components/features/inventory';

// eslint-disable-next-line import/no-default-export -- Next.js requires default export for pages
export default function AllocationsPage(): React.JSX.Element {
  return <InventoryAllocationsPage />;
}
