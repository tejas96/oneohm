import { InventoryVendorsPage as VendorsPage } from '@/components/features/inventory';

// eslint-disable-next-line import/no-default-export -- Next.js requires default export for pages
export default function InventoryVendorsRoute(): React.JSX.Element {
  return <VendorsPage />;
}
