import { ProjectTaskTemplatesPage } from '@/components/features/projects';

// eslint-disable-next-line import/no-default-export -- Next.js requires default export for pages
export default function ProjectTaskTemplates(): React.JSX.Element {
  return <ProjectTaskTemplatesPage />;
}
