import { AdvancedTableExample } from '@/components/shared/advanced-table';

/**
 * Dev / UI Preview — Advanced Table
 * Demonstrates all AdvancedTable capabilities with dummy data.
 * Not linked in navigation — visit /dev/table directly.
 */
// eslint-disable-next-line import/no-default-export -- Next.js requires default export for pages
export default function DevTablePage(): React.JSX.Element {
  return <AdvancedTableExample />;
}
