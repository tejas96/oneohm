import { InstallationPricingListPage } from '@/components/features/admin/installation-pricing';

// eslint-disable-next-line import/no-default-export -- Next.js requires default export for pages
export default function InstallationPricingPage(): React.JSX.Element {
  return <InstallationPricingListPage />;
}
